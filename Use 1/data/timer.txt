方法一：
SetTimer(1,1000,NULL);   //一个1秒触发一次的定时器 
afx_msg void OnTimer(UINT nIDEvent); //在.h中CMyDlg定义的public中加入这串代码
ON_WM_TIMER() //在消息映射里添加
void  CMyDlg::OnTimer(UINT   nIDEvent) //插入函数
{}

方法二：
SetTimer(1,1000,TimerProc);
void CALLBACK TimerProc(HWND hwnd,UINT uMsg,UINT idEvent,DWORD dwTime )//插入函数
{}

KillTimer(1);
